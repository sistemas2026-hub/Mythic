/**
 * Configuración de Prettier del monorepo.
 * Los paquetes/apps pueden re-exportar esto vía `@mythic/config/prettier`.
 */
export default {
  semi: true,
  singleQuote: true,
  trailingComma: 'all',
  printWidth: 100,
  tabWidth: 2,
  arrowParens: 'always',
};
