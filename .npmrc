# Expo/Metro no resuelve bien el node_modules aislado de pnpm.
# node-linker=hoisted crea un node_modules plano (como npm), compatible con Metro.
node-linker=hoisted

# Permite que los peerDeps nativos de Expo/React Native se resuelvan correctamente.
public-hoist-pattern[]=*
